<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.bookangel.mappers.PaymentMapper">

    <select id="subscribe">
        INSERT INTO HR.BOOK_PAYMENT
        (PAYMENTNUM, MEMBERNUM, COUPONNUM, IMPUID, APPROVALNUM, PAYMENTTYPE, SUBMONTH, SUBDATE, EXPIREDATE)
        VALUES(PAYMENT_SEQ.NEXTVAL, #{memberNum}, #{couponNum}, #{impUid}, #{approvalNum}, #{paymentType}, #{subMonth}, SYSDATE, SYSDATE+30)
    </select>

</mapper>